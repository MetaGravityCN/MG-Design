import { describe,expect,it } from 'vitest';import{mount}from'@vue/test-utils';import{MgTimePicker}from'../index'
describe('MgTimePicker',()=>{it('selects a time',async()=>{const wrapper=mount(MgTimePicker,{props:{modelValue:''}});await wrapper.find('input').setValue('09:30');expect(wrapper.emitted('update:modelValue')?.at(-1)).toEqual(['09:30'])});it('supports ranges',async()=>{const wrapper=mount(MgTimePicker,{props:{modelValue:['08:00','10:00'],isRange:true}});await wrapper.findAll('input')[0].setValue('07:30');expect(wrapper.emitted('update:modelValue')?.at(-1)).toEqual([['07:30','10:00']])})})
